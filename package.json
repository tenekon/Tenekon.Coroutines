{
  "version": "0.1.0-preview.4",
  "scripts": {
    "initial-release": "conventional-changelog -p angular -i CHANGELOG.md -s -r 0",
    "release": "conventional-changelog -p angular -i CHANGELOG.md -s"
  },
  "devDependencies": {
    "conventional-changelog-cli": "^5.0.0"
  }
}